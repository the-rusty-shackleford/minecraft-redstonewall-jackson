# 0.3.0 verification — 2026-09-24 UTC

Rusty, with 0.2.0 on the box, in front of it: "The wall mounted redstone repeater doesn't
seem to be powered properly." Read off the server by block-state queries around Jdrum12:
two repeaters on the north face of a block row, `wall=north, facing=up, powered=false`, their
input cells empty air; dust (power 9) along the row's top above the first, a torch standing
on the row above the second. Design D-0003: a wall diode also reads the block it hangs on.
Rusty: "Yes, 2. I thought that's how it would have been already."

- `./gradlew --offline --no-watch-fs test`: 16 JUnit tests, unchanged (the rule is a level
  read in the adapter; the domain is untouched).
- `./gradlew --offline --no-watch-fs runGameTestServer`: all 34 required real-server
  GameTests passed. The 24 of 0.2.0; `RepeaterGameTests` (6, written first to rule out the
  logic): a repeater placed by the item with the placer looking up, down and east along a
  north wall (the placement facing asserted each time), fed from behind by wall dust and
  read ahead by wall dust and a lamp; one above the floor dust at a wall's foot, fed by it;
  one reading a solid block that wall dust points into; one powering a solid block that wall
  dust reads beyond. All six passed before any change, so the logic was sound and Rusty's
  geometry was the question. `SupportGameTests` (4, the geometry itself): a floor run
  climbing onto a raised block, the repeater hanging on that block's far face with its
  output down into the ground and a lamp beside the ground block. Dust on top of the block:
  the repeater on and the lamp lit, then off again when the source is removed; wall dust
  into the block from another face: on; a torch standing on the block: off (a torch never
  powers what it is attached to); a comparator in the repeater's place: on.
- `./gradlew runPhotoBooth` through `tools/booth/run_iconified.sh` (iconified at pid
  1271985 while loading; nothing on Rusty's screen), sixteen photos. Judged:
  [Rusty's step](0.3.0/row-repeater.png): dust along the row's top from the redstone block,
  the repeater on the row's face below the edge with both torches lit, the lamp set in the
  ground in front of it lit. [A repeater up a wall](0.3.0/repeater-up.png): its torches lit
  in a vertical run with the lamp above lit. [A repeater along a wall](0.3.0/repeater-along.png):
  lit in the run. The texture's arrow is not readable at these sizes; direction is read from
  the torches and from the runs lighting their lamps. The thirteen photos of 0.2.0 are
  unchanged.
- `./gradlew --offline --no-watch-fs build -PskipBooth -PskipGameTests` after those runs:
  green; jar `redstonewalljackson-0.3.0.jar` SHA-1 `cbdbb71d9896077be0423d2091150c9eeaa60ec3`
  (100874 bytes). Audit: no gametest or test classes, `version="0.3.0"`.
- Not verified: Rusty's own build on the box lighting up after the restart; that is the
  first thing to look at.
